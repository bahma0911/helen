# Helen Wine Shop — Frontend

Vite + React + Tailwind frontend.

Install and run:

```bash
cd frontend
npm install
npm run dev
```

Change the backend API base URL by setting `VITE_API_BASE` in your environment or `.env` file.
